# Copy this file to .env and fill in your credentials.
# All variables are optional — the app runs in localStorage demo mode without Supabase,
# and falls back to simulated price data without Finnhub.

# ── Supabase (optional) ──────────────────────────────────────────────────────
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ── Finnhub real-time market data (optional) ─────────────────────────────────
# Free tier at https://finnhub.io — grants 60 API calls/min, WebSocket trades.
# Leave blank to use simulated price history from mockData.js.
VITE_FINNHUB_API_KEY=

# ── Email (optional) ─────────────────────────────────────────────────────────
# Invite emails are sent via a Supabase Edge Function (send-invite-email).
# If the Edge Function is not deployed, the app falls back to opening the
# user's email client with a pre-filled mailto: link.
# To set up: deploy the send-invite-email Edge Function in your Supabase project.
